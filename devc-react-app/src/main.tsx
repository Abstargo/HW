import React from 'react'
import ReactDOM from 'react-dom/client'
import TypesOfFood from './TASK10' 

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TypesOfFood />
  </React.StrictMode>,
)
